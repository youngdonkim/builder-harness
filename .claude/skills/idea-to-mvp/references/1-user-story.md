---
name: 1-user-story
description: 유저 스토리 단계 가이드. 필수 입력(단면 4개·양면 시장이면 5개)을 걷어 user-scenario-writer 에이전트로 영화 시나리오 형식의 유저 스토리를 생성·저장. idea-to-mvp SKILL.md §2의 1번 단계.
---

이 단계는 7단계 전체의 출발점이다. 아직 검증되지 않은 아이디어를 **영화 시나리오 형식의 구체적인 그림**으로 먼저 그려서, 이후 단계에 넘길 맥락의 해상도를 높인다. **이 단계는 통과 기준(게이트) 아님** — 거르기는 마지막 단계(MvpLaunch)의 광고 실측에서 한 번만 한다.

## 목차

1. [단계 목표](#1-단계-목표)
2. [진행 절차](#2-진행-절차)
   - [2.1 단계 시작](#21-단계-시작--이번-단계가-할-일을-안내한다-7단계의-첫-단계라-이전-정리는-없음)
   - [2.2 필수 입력 받기](#22-필수-입력-받기-인터뷰-모드)
   - [2.3 에이전트 위임](#23-에이전트-위임)
   - [2.4 결과 보고·리뷰](#24-결과-보고리뷰)
   - [2.5 스케치에서 되돌아온 경우](#25-스케치에서-되돌아온-경우)
3. [완료 체크리스트](#3-완료-체크리스트)
   - [3.1 다음 단계 핸드오프](#31-다음-단계-핸드오프)
4. [산출물 스펙](#4-산출물-스펙)
   - [4.1 `# 입력 (확정)` 섹션 템플릿](#41--입력-확정-섹션-템플릿)
5. [사용자 응대 톤](#5-사용자-응대-톤)

---

## 1. 단계 목표

이 단계의 목표는 **아이디어의 초기 맥락(필수 입력 — 단면 4개, 양면 시장이면 5개)을 사용자에게서 걷고, `user-scenario-writer` 서브 에이전트에 위임해 영화 시나리오 형식의 유저 스토리를 만드는 것**이다. 산출물은 `mvp/user-story.md` 한 파일 — 위임 프롬프트 그대로 입력 원문(§4.1)과 에이전트가 생성한 시나리오 본문을 담는다.

**바로 다음 단계는 2단계(Sketch)다.** 스케치는 이 문서에서 기능 목록을 뽑고, 여기 적힌 화면 묘사·문구를 그대로 스펙 삼아 클릭 스케치를 만든다 (`2-sketch.md`). 그 스케치를 눌러보다 이 문서의 결함이 드러나면 되돌아와 고친다 — 그래서 이 문서는 스케치가 수렴할 때까지 살아 있는 산출물이다 (§2.5).

스케치를 지나면 이 문서는 뒤 단계로 계속 이어진다 — 3단계(InformationArchitecture)는 이 문서와 기능 목록에서 화면 인벤토리·객체 지도를 뽑아내고(`3-information-architecture.md`), 4단계(Prototype)는 그 인벤토리와 이 문서를 함께 Claude Design에 입력해 목업의 톤·문구를 잡는다 (`4-prototype.md`). 5단계(MarketResearch)는 이 문서의 대안·타겟 정보를 경쟁자 조사의 출발점으로 삼고 (`5-market-research.md`), 7단계(MvpLaunch)는 이 문서와 실제 앱 코드를 근거로 최종 확정 아이디어를 못 박는다 (`7-mvp-launch.md` §2.1.1). 목표를 이뤘는지는 §3 완료 체크리스트로 판정한다.

## 2. 진행 절차

### 2.1 단계 시작 — 이번 단계가 할 일을 안내한다 (7단계의 첫 단계라 이전 정리는 없음)

이 단계는 7단계의 첫 단계라 "이전 단계까지 정리된 내용"이 없다 — 대신 사용자에게 **왜 유저 스토리부터 쓰는지**를 짧게 설명하는 것으로 시작한다.

설명 골자 — 그대로 또는 풀어서 전달:

> 서비스를 영화 한 편처럼 구체적인 그림으로 먼저 그려두면, 이후 단계에서 AI에게 넘길 맥락의 해상도가 올라가.

이미 `mvp/user-story.md`가 있으면(스킬 중간 재실행 등) 읽고 §3 체크리스트로 검증해 부족한 부분만 보완한다 — 처음부터 다시 받지 않는다.

### 2.2 필수 입력 받기 (인터뷰 모드)

받을 입력은 **기본 4개**다. 파는 쪽·사는 쪽이 따로 있는 **양면 시장**일 때만 갈라진다 — 그건 아래 "양면 시장이면 여기서 갈린다"에서 다룬다.

이 절차는 SKILL.md §1.5의 **인터뷰 모드** — 사용자의 암묵지(아직 아무도 모르는 자기 아이디어)가 소재라 상호작용 자체가 목적. 아래 4개를 그대로 또는 풀어서 사용자에게 안내:

```
서비스를 시작하기 전에 네 가지만 알려줘:

1. 서비스 이름과 한 줄 정의
2. 타겟 — 누가, 어떤 상황에서
3. 현재의 대안과 그 불편함 (문제)
4. 해결책의 핵심 아이디어
```

**입력 1·2는 CLAUDE.md로 미리 채운다** — 프로젝트 CLAUDE.md 상단에 프로젝트 정의(서비스 이름·한 줄 정의·타겟)가 이미 있으면(project-init 인터뷰가 채워둠), 입력 1·2를 그 내용으로 채워 보여주고 맞는지 확인만 받는다. 새로 묻는 건 3·4다. 타겟의 "어떤 상황에서"가 CLAUDE.md에 없으면 그 부분만 보충해 받는다.

코칭 규칙 (SKILL.md §1.4):

- **빠짐·모호는 배치로 되묻기** — 하나씩 직렬로 왕복하지 말고 모자란 항목 2~4개를 한 턴에 묶어 확인.
- **"잘 모르겠어"는 정상 답** — AI가 후보 2~3개 제시하고 사용자가 골라 다듬게 한다.

**양면 시장이면 여기서 갈린다** — 쓰는 사람이 두 종류이고 **한쪽이 있어야 다른 쪽이 쓸 수 있는** 서비스가 양면 시장이다 (중고 거래 — 파는 사람·사는 사람 / 배달 — 가게·손님 / 강의 마켓 — 강사·수강생). 판별은 **입력 1(한 줄 정의)에서 감지**한다. 질문을 하나 더 늘리지 말고, 위 배치 되묻기에 얹어 확인만 받는다:

```
이거 쓰는 사람이 두 종류 맞지? — {A 역할}이랑 {B 역할}.
맞으면 2·3·4번은 양쪽 각각 받을게.
```

- **단면이면** 위 4개 그대로다. 아무것도 안 바뀐다.
- **양면이면** 2·3·4를 역할별로 받고, 입력 5를 하나 더 받는다:

```
양쪽이 있으니 이렇게 받을게:

2. 타겟 — {A 역할}은 누가·어떤 상황에서 / {B 역할}은 누가·어떤 상황에서
3. 현재의 대안과 그 불편함 — {A 역할}은 지금 어떻게 때우는지 / {B 역할}은 지금 어떻게 때우는지
4. 해결책의 핵심 아이디어 — {A 역할}에게 뭘 해주는지 / {B 역할}에게 뭘 해주는지
5. 초기 공급을 어떻게 채울 건가 — 어느 쪽부터 채울지, 그 한쪽을 직접 채울 건지
```

역할이 둘이어도 **되묻기는 한 턴에 묶는다** — 왕복을 두 배로 늘리지 않는다. 칸이 6개(2·3·4 × 두 역할)라 한꺼번에 답하기 버거워하면 "2·3번 먼저, 4·5번 다음"처럼 두 턴까지만 나눈다.

**왜 2·3·4를 쪼개나** — 파는 쪽 고민과 사는 쪽 고민은 아예 다른 얘기다. 한쪽만 적혀 있으면 화면도 그 한쪽만 생기고, 결국 7단계에서 그 한쪽만 검증하게 된다.

**왜 5번을 묻나 — "닭이 먼저냐 달걀이 먼저냐"** — 양면 시장의 제일 큰 위험은 한쪽이 없으면 다른 쪽도 안 온다는 것이다. 살 사람이 없으면 파는 사람이 안 올리고, 올라온 게 없으면 살 사람이 안 온다. 그래서 초기엔 보통 **한쪽을 만드는 사람이 직접 채운다** — 물건을 직접 구해다 올리거나, 공급자를 발로 뛰어 섭외하거나, 한 동네·한 학교로 범위를 좁혀 양쪽을 동시에 채우거나. 이건 조사로 알아낼 일이 아니라 만드는 사람이 정할 일이라 여기서 묻는다. "잘 모르겠어"면 위 코칭 규칙대로 후보 2~3개를 제시해 고르게 한다.

**입력이 다 확정되기 전에는 에이전트에 위임하지 않는다** (단면 4개·양면 5개). `user-scenario-writer`는 빠진 입력을 임의로 가정해 채우는 폴백을 갖고 있지만, 그건 강행 진입용 안전판이지 기본 경로가 아니다 — 전부 사용자 확인을 받은 뒤에만 §2.3으로 넘어간다.

### 2.3 에이전트 위임

입력이 확정되면 `user-scenario-writer` 서브 에이전트를 호출해 위임한다. 위임 프롬프트에 반드시 포함할 것:

- **(a) 확정된 입력 원문 전부** — 요약·재구성 없이 사용자가 확인한 그대로 전달 (단면 4개·양면 5개, 양면이면 2·3·4의 역할별 답 전부).
- **(b) 저장 경로 명시** — `mvp/user-story.md` (에이전트 기본 경로와 동일 — `.claude/agents/user-scenario-writer.md` §출력).
- **(c) 문서 상단 `# 입력 (확정)` 섹션 보존 지시** — 입력 원문을 문서 최상단에 그 섹션으로 남기라고 명시. 뒤 단계들이 여기를 아이디어의 출발점으로 읽는다 (5단계 경쟁자 조사, 7단계 최종 확정).
- **(d) 양면이면 그 사실과 두 역할 이름** — 어느 쪽이 수요(사는 쪽)이고 어느 쪽이 공급(파는 쪽)인지 적어 넘긴다. 에이전트는 주인공을 수요 쪽으로 잡고 반대편 장면을 2~3개 그린다 (`.claude/agents/user-scenario-writer.md` §필수 구조).

이미 `mvp/user-story.md`가 있으면 에이전트 자체 규칙대로 `user-story-v2.md`처럼 버전을 올려 저장된다 — 스킬 쪽에서 별도 처리 안 함.

### 2.4 결과 보고·리뷰

에이전트가 반환하는 세 가지 — **저장 경로 · 로그라인(logline — 서비스의 정서를 담은 한 줄 요약) · 장면 목록** — 를 사용자에게 그대로 보여주고 피드백을 받는다.

이건 SKILL.md §1.5의 **초안 모드** — AI(서브 에이전트)가 산출물을 완주해서 만든 뒤 완성본으로 한 번에 리뷰받는 작업이다. 중간에 장면 하나하나 승인받지 않는다. 수정 요청이 오면 무엇을 바꿀지 구체화해 에이전트 재위임으로 반영하고, 다시 세 가지를 보여준다.

### 2.5 스케치에서 되돌아온 경우

**2단계 스케치에서 이 문서의 결함이 드러나면 이 절차로 되돌아온다.** 클릭 스케치를 실제로 눌러보면 글로만 읽을 땐 안 보이던 것들이 드러난다 — 아무 일도 안 하는 사족 문구, 장면과 장면 사이에 빠져 있던 화면, 실제로는 성립하지 않는 전제 같은 것들. 이럴 땐 `user-scenario-writer` 에이전트를 **재위임**해 고친다 (§2.3과 같은 방식, 무엇을 왜 바꾸는지를 위임 프롬프트에 적는다). 새 판본은 `user-story-v2.md`처럼 번호가 올라가고 옛 판본은 남는다 (SKILL.md §3).

고친 뒤에는 2단계로 돌아가 기능 목록과 스케치를 다시 맞춘다. 이 왕복이 **1↔2 순환**이고, 3단계 IA가 이 문서를 읽어 가는 순간 순환이 끝나며 이 문서는 기록으로 얼려진다.

## 3. 완료 체크리스트

- [ ] **필수 입력 확정** (§2.2) — 단면 4개·양면 5개(양면은 2·3·4가 역할별로 갈림). 사용자 확인 완료, 배치 되묻기로 빠짐·모호 해소.
- [ ] **`user-story.md` 저장** (§2.3) — `mvp/`, 상단 `# 입력 (확정)` 섹션에 입력 원문 포함.
- [ ] **로그라인·장면 목록 사용자 확인** (§2.4) — 수정 요청 있으면 재위임까지 반영 완료.
- [ ] **디자인 방향 절 포함** — 4단계 권장 프롬프트가 이 절을 "시각 디자인의 원천"으로 가리킨다. 없으면 디자인 도구가 씬 묘사를 문자 그대로 그려 밋밋한 목업이 나온다 (실제 사고 있었음).

### 3.1 다음 단계 핸드오프

산출물 저장 직후, 매 단계 공통대로 **`/clear` 후 `/idea-to-mvp` 재호출**을 권장한다 (SKILL.md §3.2). 다음 단계는 **2단계(Sketch)**다 (`2-sketch.md`) — 이 문서에서 기능 목록을 뽑아 스토리 밖(반대편 역할·운영자 기능·법/돈)까지 훑고, html/css/js만으로 클릭 스케치를 만들어 기획자가 직접 눌러본다. 눌러보다 이 문서의 결함이 드러나면 §2.5로 되돌아온다.

그 뒤 3단계(InformationArchitecture)가 이 문서와 기능 목록을 함께 읽어 화면 인벤토리·객체 지도·상태 매트릭스를 뽑아낸다 (`3-information-architecture.md`) — 유저 스토리는 주인공 한 명의 성공 경로 하나라, 거기서 안 그려진 화면(빈 상태·실패 분기·설정 등)까지 3단계가 채워 서비스 전체 지도를 만든다. 4·5·7단계가 이 문서를 어떻게 쓰는지는 §1에 정리돼 있다.

## 4. 산출물 스펙

위치: 프로젝트 루트의 `mvp/user-story.md`

본문 구조는 `user-scenario-writer` 에이전트의 필수 구조 — 타이틀+로그라인 · 주인공 페르소나 · 장면(S#) 구성 · "시나리오에 심어둔 UX 장치 정리" 표 · 디자인 방향 — 를 그대로 따른다 (`.claude/agents/user-scenario-writer.md` §필수 구조). 여기서 중복 정의하지 않는다. 이 스킬이 그 위에 추가로 요구하는 건 문서 최상단 `# 입력 (확정)` 섹션 하나뿐 — §2.3(c).

### 4.1 `# 입력 (확정)` 섹션 템플릿

**단면(기본)**:

```markdown
# 입력 (확정)

1. **서비스 이름과 한 줄 정의**: {§2.2에서 받은 원문}
2. **타겟**: {누가, 어떤 상황에서}
3. **현재의 대안과 불편함 (문제)**: {원문}
4. **해결책의 핵심 아이디어**: {원문}
```

**양면 시장**: 번호는 그대로 두고 2·3·4 안에서만 역할별로 갈라 적는다. 뒤 단계들이 이 섹션을 번호로 읽으니 번호 체계를 바꾸지 않는다.

```markdown
# 입력 (확정)

> 양면 시장 — 수요 쪽: {B 역할} / 공급 쪽: {A 역할}

1. **서비스 이름과 한 줄 정의**: {원문}
2. **타겟**
   - {A 역할}: {누가, 어떤 상황에서}
   - {B 역할}: {누가, 어떤 상황에서}
3. **현재의 대안과 불편함 (문제)**
   - {A 역할}: {원문}
   - {B 역할}: {원문}
4. **해결책의 핵심 아이디어**
   - {A 역할}: {원문}
   - {B 역할}: {원문}
5. **초기 공급을 채우는 방법**: {어느 쪽부터, 직접 채울지 — 원문}
```

이 뒤로 `user-scenario-writer`가 생성한 시나리오 본문이 이어진다.

## 5. 사용자 응대 톤

- **톤**: SKILL.md §1.3 따름 — 반말·친근·짧게.
- **흐름**: 필수 입력 받기(§2.2) → 에이전트 위임(§2.3) → 결과 리뷰(§2.4). 단계 건너뛰지 않기 — 특히 입력이 다 확정되기 전에 위임부터 하는 점프 금지.
- **솔루션 함정 주의**: 입력 ④(해결책의 핵심 아이디어)는 받되, 그 검증은 마지막 7단계(MvpLaunch) 광고 실측의 몫이다. 유저 스토리는 아직 검증 전의 그림일 뿐 — 사용자가 "이 시나리오대로 확정됐다"고 오해하지 않도록, 결과 리뷰(§2.4) 때 "이건 앞으로 만들면서 다듬어갈 그림이고, 맞는지는 마지막에 실제 사용자 반응으로 확인한다"고 짚어준다.
- **시나리오 품질 불만**: 사용자가 "장면이 별로다", "와닿지 않는다" 하면 에이전트한테 "다시 잘 써줘"만 던지지 말고, 입력 중 무엇이 모호했는지 되짚어 더 구체화한 뒤 재위임한다.
