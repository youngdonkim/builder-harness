# builder-harness

AI 빌더를 위한 Claude Code 하네스의 원본 저장소. 각 프로젝트는 이 저장소의 payload를 파일로 복사해 쓴다.

- 이 저장소의 작업 흐름(브랜치·PR)은 README를 따른다.
- **payload가 원본** — 스킬·에이전트·rules 수정은 `payload/.claude/`에 하고, 루트 `.claude/`(이 저장소 자신에게 적용된 미러)에 같은 파일을 복사해 동기화한다.

## 작업 원칙 (MUST FOLLOW)

### 위임

- 코딩과 문서 작성은 직접 하지 않고 저성능 하위 모델(opus, sonnet, haiku)을 적절히 판단해 선택해서 서브 에이전트에 위임한다. `.claude/agents/`에 맞는 에이전트가 있으면 그걸 쓰고, 위임할 때 어떤 모델의 에이전트인지 사용자에게 알린다.
- **재위임 금지** — 위임받은 서브 에이전트는 다시 서브 에이전트를 띄우지 않고 직접 실행한다. 이 위임 원칙은 메인 세션용이고, 위임받은 쪽은 그 위임을 받은 실행자다. 작업이 커서 쪼개야 하면 그건 메인 에이전트 몫이다 — 메인이 여러 에이전트를 한꺼번에 띄우는 건 재위임이 아니라 정상이고, 금지되는 건 서브가 스스로 또 위임하는 것이다. (부모는 자기 밑 자식이 다 끝나야 닫히는 구조라, 자식이 멈추면 일을 마치고 결과까지 낸 부모도 함께 매달린다 — 실제로 1시간 20분 매달리고 강제 종료도 안 먹힌 적이 있다.)
- **배포는 위임 금지** — 배포처럼 결과가 외부로 나가는 작업은 위임하지 않고 메인 에이전트가 직접 한다. 사람이 중간에 낀 채로 실행되는 게 더 안전해서다.

### 응답·문서 작성

- **친근한 반말** — 응답과 문서 모두 친근한 반말로 쓴다.
- **조어 금지** — 스스로 만든 신조어·축약어를 쓰지 않는다. 이미 널리 쓰이는 쉬운 말로 표현한다.
- **전문용어 표기** — 한국어가 아닌 업계 전문용어는 `번역(외국어 표기)` 형식으로 쓰고, 처음 나올 때 한 번은 쉽게 풀어 설명한다. 예: "닻 내림(anchoring) — 처음 본 정보에 판단이 묶이는 심리 현상".
- **간결하되, 충돌하면 쉬움 우선** — 핵심만 간결하게, 쉽게 풀어 쓴다. 간결함과 쉬움이 충돌하면 중학생도 알아들을 수 있는 쉬운 쪽을 택한다. 판단 순서: 핵심 아니면 뺀다 → 쉽게 짧게 되면 짧게 쓴다 → 짧게 쓰면 어려워질 땐 길어져도 풀어 쓴다.
